# Cloud Mail 第二阶段

本阶段加入：

- 每个发件账号独立的富文本签名，可控制是否启用、回复时是否加入；后端会清理危险 HTML。
- 可选的标准已读回执请求（`Disposition-Notification-To`），收到 MDN 后写入邮件追踪时间线。
- 附件改为预上传并以一次性令牌发送，避免 Base64 JSON 的内存与体积开销；单文件 20 MB、合计 25 MB、最多 10 个。
- 保留旧 Base64 附件格式的发送兼容性，便于已有草稿逐步迁移。

升级后以管理员身份调用一次 `POST /api/admin/migrate`。全新安装可调用 `POST /api/init`，并通过 `x-init-secret` 请求头传入密钥。

已读回执无法强制：收件邮箱可以不支持、拒绝或询问用户后才发送。MDN 也不是加密身份证明；像素追踪的 IP 仍可能属于邮件隐私代理或缓存。
